// Every module is in its own file. Modules are used to call into
// main program.

#![allow(dead_code)] //Ignore warnings for previous code that isn't called

pub mod pascals_triangle;
pub mod reverse_integer;